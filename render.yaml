# render.yaml
services:
  - type: web
    name: office-view
    env: python
    buildCommand: |
      ./build.sh
      pip install -r requirements.txt
    startCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"
    autoDeploy: false
